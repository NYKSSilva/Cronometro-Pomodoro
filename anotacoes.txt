    //o return nesse tipo de programação necessia de uma div "vazia" ("<> </>") para fazer todo o html dentro dela
    // & - Intersection Type, o componente aceita:
    // propriedades obrigatórias (id e labelText)
    // TODAS as propriedades padrão que um elemento <input> nativo HTML aceita
    // (value , on Change, disabel, placeholder, step, etc.)
    // ; Aceita por causa do React.Component <'input'>

    // ... = spread operator (espalhamento, rest parameters)
    // Qual é o papel de ... 
    // no argumento ele funciona como rest, informa ao react "pega todas as
    // propriedades que sobram (que não são id, type ou labelText)" e coloque 
    // dentro  de um objeto de rest no elemento <input> 
    // 
    //
    //                                    CHILDREN
    //o children é utilizado no codigo para poder atribuir algo ao componente quando chama-lo,  como por exemplo:
    // dentro da pasta app "<Exemplo>Final</Exemplo>"se o componente tiver children dentro dele a palavra que foi
    // escrita no meio do componente qunado chamado aparece dentro do componente. Isso deixa o 
    //componente flexivel para que consiga utilizar o mesmo componete para coisas diferente que usam a mesma base